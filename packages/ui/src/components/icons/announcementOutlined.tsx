import { AnnouncementOutlined } from "@mui/icons-material";

import { generateIcon } from "./utils";

export const AnnouncementOutlinedIcon = generateIcon({
	Icon: AnnouncementOutlined,
	type: "mui",
});
